// This file was generated by prisma-generator-effect, do not edit manually.

import { Data, Effect, Exit, Layer, Option, Scope, Context } from "effect";
import type {
	Effect as EffectType,
	Error as EffectError,
	Services as EffectServices,
	Success as EffectSuccess,
} from "effect/Effect";
import {
	Prisma as PrismaNamespace,
	PrismaClient as BasePrismaClient,
} from "../client/client.js";

// Create local reference to error class for proper type narrowing
const PrismaClientKnownRequestError =
	PrismaNamespace.PrismaClientKnownRequestError;

// Type guard function to help TypeScript narrow the error type
// This is needed for Prisma 7 which re-exports error classes from external packages
function isPrismaClientKnownRequestError(
	error: unknown,
): error is PrismaNamespace.PrismaClientKnownRequestError {
	return error instanceof PrismaClientKnownRequestError;
}

// ============================================================================
// Type aliases for model operations (performance optimization)
// These are computed once and reused, reducing TypeScript's type-checking workload
// ============================================================================

type UserFindUniqueArgs = PrismaNamespace.Args<
	BasePrismaClient["user"],
	"findUnique"
>;
type UserFindUniqueOrThrowArgs = PrismaNamespace.Args<
	BasePrismaClient["user"],
	"findUniqueOrThrow"
>;
type UserFindFirstArgs = PrismaNamespace.Args<
	BasePrismaClient["user"],
	"findFirst"
>;
type UserFindFirstOrThrowArgs = PrismaNamespace.Args<
	BasePrismaClient["user"],
	"findFirstOrThrow"
>;
type UserFindManyArgs = PrismaNamespace.Args<
	BasePrismaClient["user"],
	"findMany"
>;
type UserCreateArgs = PrismaNamespace.Args<BasePrismaClient["user"], "create">;
type UserCreateManyArgs = PrismaNamespace.Args<
	BasePrismaClient["user"],
	"createMany"
>;
type UserCreateManyAndReturnArgs = PrismaNamespace.Args<
	BasePrismaClient["user"],
	"createManyAndReturn"
>;
type UserDeleteArgs = PrismaNamespace.Args<BasePrismaClient["user"], "delete">;
type UserUpdateArgs = PrismaNamespace.Args<BasePrismaClient["user"], "update">;
type UserDeleteManyArgs = PrismaNamespace.Args<
	BasePrismaClient["user"],
	"deleteMany"
>;
type UserUpdateManyArgs = PrismaNamespace.Args<
	BasePrismaClient["user"],
	"updateMany"
>;
type UserUpdateManyAndReturnArgs = PrismaNamespace.Args<
	BasePrismaClient["user"],
	"updateManyAndReturn"
>;
type UserUpsertArgs = PrismaNamespace.Args<BasePrismaClient["user"], "upsert">;
type UserCountArgs = PrismaNamespace.Args<BasePrismaClient["user"], "count">;
type UserAggregateArgs = PrismaNamespace.Args<
	BasePrismaClient["user"],
	"aggregate"
>;
type UserGroupByArgs = PrismaNamespace.Args<
	BasePrismaClient["user"],
	"groupBy"
>;

type PostFindUniqueArgs = PrismaNamespace.Args<
	BasePrismaClient["post"],
	"findUnique"
>;
type PostFindUniqueOrThrowArgs = PrismaNamespace.Args<
	BasePrismaClient["post"],
	"findUniqueOrThrow"
>;
type PostFindFirstArgs = PrismaNamespace.Args<
	BasePrismaClient["post"],
	"findFirst"
>;
type PostFindFirstOrThrowArgs = PrismaNamespace.Args<
	BasePrismaClient["post"],
	"findFirstOrThrow"
>;
type PostFindManyArgs = PrismaNamespace.Args<
	BasePrismaClient["post"],
	"findMany"
>;
type PostCreateArgs = PrismaNamespace.Args<BasePrismaClient["post"], "create">;
type PostCreateManyArgs = PrismaNamespace.Args<
	BasePrismaClient["post"],
	"createMany"
>;
type PostCreateManyAndReturnArgs = PrismaNamespace.Args<
	BasePrismaClient["post"],
	"createManyAndReturn"
>;
type PostDeleteArgs = PrismaNamespace.Args<BasePrismaClient["post"], "delete">;
type PostUpdateArgs = PrismaNamespace.Args<BasePrismaClient["post"], "update">;
type PostDeleteManyArgs = PrismaNamespace.Args<
	BasePrismaClient["post"],
	"deleteMany"
>;
type PostUpdateManyArgs = PrismaNamespace.Args<
	BasePrismaClient["post"],
	"updateMany"
>;
type PostUpdateManyAndReturnArgs = PrismaNamespace.Args<
	BasePrismaClient["post"],
	"updateManyAndReturn"
>;
type PostUpsertArgs = PrismaNamespace.Args<BasePrismaClient["post"], "upsert">;
type PostCountArgs = PrismaNamespace.Args<BasePrismaClient["post"], "count">;
type PostAggregateArgs = PrismaNamespace.Args<
	BasePrismaClient["post"],
	"aggregate"
>;
type PostGroupByArgs = PrismaNamespace.Args<
	BasePrismaClient["post"],
	"groupBy"
>;

// Symbol used to identify intentional rollbacks vs actual errors
const ROLLBACK = Symbol.for("prisma.effect.rollback");

// Type for the flat transaction client with commit/rollback control
type FlatTransactionClient = PrismaNamespace.TransactionClient & {
	$commit: () => Promise<void>;
	$rollback: () => Promise<void>;
};

/** Transaction options for $transaction and $isolatedTransaction */
type TransactionOptions = {
	maxWait?: number;
	timeout?: number;
	isolationLevel?: PrismaNamespace.TransactionIsolationLevel;
};

/**
 * Context tag for the Prisma client instance.
 *
 * Use `PrismaClient.layer()` or `PrismaClient.layerEffect()` to create a layer.
 *
 * @example
 * // Prisma 7 - adapter or accelerateUrl is required
 * const layer = PrismaClient.layer({ adapter: myAdapter })
 *
 * // With transaction options (Prisma uses these as defaults for $transaction)
 * const layer = PrismaClient.layer({
 *   adapter: myAdapter,
 *   transactionOptions: { isolationLevel: "Serializable", timeout: 10000 }
 * })
 */
export class PrismaClient extends Context.Service<
	PrismaClient,
	BasePrismaClient
>()("PrismaClient") {
	/**
	 * Create a PrismaClient layer with the given options.
	 * The client will be automatically disconnected when the layer scope ends.
	 *
	 * Pass options directly - the signature matches PrismaClient's constructor.
	 * Prisma 7: requires either `adapter` or `accelerateUrl`
	 *
	 * @example
	 * // Prisma 7 with adapter
	 * const layer = PrismaClient.layer({ adapter: myAdapter })
	 *
	 * // With transaction options
	 * const layer = PrismaClient.layer({
	 *   adapter: myAdapter,
	 *   transactionOptions: { isolationLevel: "Serializable" }
	 * })
	 */
	static layer: (
		...args: ConstructorParameters<typeof BasePrismaClient>
	) => Layer.Layer<PrismaClient, never, never> = (...args) =>
		Layer.effect(
			PrismaClient,
			Effect.gen(function* () {
				const prisma: BasePrismaClient = new BasePrismaClient(...args);
				yield* Effect.addFinalizer(() =>
					Effect.promise(() => prisma.$disconnect()),
				);
				return prisma;
			}),
		);

	/**
	 * Create a PrismaClient layer where options are computed via an Effect.
	 * Useful when you need to fetch configuration from environment, config service, or create adapters.
	 * The client will be automatically disconnected when the layer scope ends.
	 *
	 * @example
	 * // Get config from a service
	 * const layer = PrismaClient.layerEffect(
	 *   Effect.gen(function* () {
	 *     const config = yield* ConfigService
	 *     return { adapter: createAdapter(config.databaseUrl) }
	 *   })
	 * )
	 *
	 * // With transaction options
	 * const layer = PrismaClient.layerEffect(
	 *   Effect.gen(function* () {
	 *     return {
	 *       adapter: myAdapter,
	 *       transactionOptions: { isolationLevel: "Serializable" }
	 *     }
	 *   })
	 * )
	 */
	static layerEffect: <R, E>(
		optionsEffect: EffectType<
			ConstructorParameters<typeof BasePrismaClient>[0],
			E,
			R
		>,
	) => Layer.Layer<PrismaClient, E, Exclude<R, Scope.Scope>> = (
		optionsEffect,
	) =>
		Layer.effect(
			PrismaClient,
			Effect.gen(function* () {
				const options: ConstructorParameters<typeof BasePrismaClient>[0] =
					yield* optionsEffect;
				const prisma: BasePrismaClient = new BasePrismaClient(options);
				yield* Effect.addFinalizer(() =>
					Effect.promise(() => prisma.$disconnect()),
				);
				return prisma;
			}),
		);
}

/**
 * Context tag for the transaction client within a transaction scope.
 * This service is only available inside `$transaction` calls.
 * Use `Effect.serviceOption(PrismaTransactionClientService)` to check if you're in a transaction.
 */
export class PrismaTransactionClientService extends Context.Service<
	PrismaTransactionClientService,
	PrismaNamespace.TransactionClient
>()("PrismaTransactionClientService") {}

/**
 * Context tag for read replica clients.
 *
 * When provided alongside `PrismaClient`, read operations (`findUnique`, `findFirst`,
 * `findMany`, `count`, `aggregate`, `groupBy`, etc.) are routed to a random replica.
 * Write operations, transactions, and raw SQL always use the primary client by default.
 *
 * Use `prisma.$primary()` / `prisma.$replica()` on the service to force routing.
 *
 * @example
 * const AppLayer = Layer.mergeAll(
 *   Prisma.layer({ adapter: primaryAdapter }),
 *   PrismaReplicas.layer([{ adapter: replica1Adapter }, { adapter: replica2Adapter }])
 * )
 */
export class PrismaReplicas extends Context.Service<
	PrismaReplicas,
	ReadonlyArray<BasePrismaClient>
>()("PrismaReplicas") {
	/**
	 * Create a PrismaReplicas layer from an array of PrismaClient constructor options.
	 * Each replica client will be automatically disconnected when the layer scope ends.
	 */
	static layer: (
		configs: ReadonlyArray<ConstructorParameters<typeof BasePrismaClient>[0]>,
	) => Layer.Layer<PrismaReplicas, never, never> = (configs) =>
		Layer.effect(
			PrismaReplicas,
			Effect.gen(function* () {
				const clients: ReadonlyArray<BasePrismaClient> = configs.map(
					(c) => new BasePrismaClient(c),
				);
				yield* Effect.addFinalizer(() =>
					Effect.promise(() =>
						Promise.all(clients.map((c) => c.$disconnect())).then(
							() => undefined,
						),
					),
				);
				return clients;
			}),
		);

	/**
	 * Create a PrismaReplicas layer from an Effect that yields the replica configs.
	 * Useful when replica configuration depends on an Effect service (e.g. config lookup).
	 */
	static layerEffect: <R, E>(
		configsEffect: EffectType<
			ReadonlyArray<ConstructorParameters<typeof BasePrismaClient>[0]>,
			E,
			R
		>,
	) => Layer.Layer<PrismaReplicas, E, Exclude<R, Scope.Scope>> = (
		configsEffect,
	) =>
		Layer.effect(
			PrismaReplicas,
			Effect.gen(function* () {
				const configs = yield* configsEffect;
				const clients: ReadonlyArray<BasePrismaClient> = configs.map(
					(c) => new BasePrismaClient(c),
				);
				yield* Effect.addFinalizer(() =>
					Effect.promise(() =>
						Promise.all(clients.map((c) => c.$disconnect())).then(
							() => undefined,
						),
					),
				);
				return clients;
			}),
		);

	/**
	 * Create a PrismaReplicas layer from an array of already-constructed PrismaClient instances.
	 * The caller is responsible for managing the lifecycle of the provided clients.
	 */
	static layerFromClients: (
		clients: ReadonlyArray<BasePrismaClient>,
	) => Layer.Layer<PrismaReplicas, never, never> = (clients) =>
		Layer.succeed(PrismaReplicas, clients);
}

export class PrismaUniqueConstraintError extends Data.TaggedError(
	"PrismaUniqueConstraintError",
)<{
	cause: PrismaNamespace.PrismaClientKnownRequestError;
	operation: string;
	model: string;
}> {}

export class PrismaForeignKeyConstraintError extends Data.TaggedError(
	"PrismaForeignKeyConstraintError",
)<{
	cause: PrismaNamespace.PrismaClientKnownRequestError;
	operation: string;
	model: string;
}> {}

export class PrismaRecordNotFoundError extends Data.TaggedError(
	"PrismaRecordNotFoundError",
)<{
	cause: PrismaNamespace.PrismaClientKnownRequestError;
	operation: string;
	model: string;
}> {}

export class PrismaRelationViolationError extends Data.TaggedError(
	"PrismaRelationViolationError",
)<{
	cause: PrismaNamespace.PrismaClientKnownRequestError;
	operation: string;
	model: string;
}> {}

export class PrismaRelatedRecordNotFoundError extends Data.TaggedError(
	"PrismaRelatedRecordNotFoundError",
)<{
	cause: PrismaNamespace.PrismaClientKnownRequestError;
	operation: string;
	model: string;
}> {}

export class PrismaTransactionConflictError extends Data.TaggedError(
	"PrismaTransactionConflictError",
)<{
	cause: PrismaNamespace.PrismaClientKnownRequestError;
	operation: string;
	model: string;
}> {}

export class PrismaValueTooLongError extends Data.TaggedError(
	"PrismaValueTooLongError",
)<{
	cause: PrismaNamespace.PrismaClientKnownRequestError;
	operation: string;
	model: string;
}> {}

export class PrismaValueOutOfRangeError extends Data.TaggedError(
	"PrismaValueOutOfRangeError",
)<{
	cause: PrismaNamespace.PrismaClientKnownRequestError;
	operation: string;
	model: string;
}> {}

export class PrismaDbConstraintError extends Data.TaggedError(
	"PrismaDbConstraintError",
)<{
	cause: PrismaNamespace.PrismaClientKnownRequestError;
	operation: string;
	model: string;
}> {}

export class PrismaConnectionError extends Data.TaggedError(
	"PrismaConnectionError",
)<{
	cause: PrismaNamespace.PrismaClientKnownRequestError;
	operation: string;
	model: string;
}> {}

export class PrismaMissingRequiredValueError extends Data.TaggedError(
	"PrismaMissingRequiredValueError",
)<{
	cause: PrismaNamespace.PrismaClientKnownRequestError;
	operation: string;
	model: string;
}> {}

export class PrismaInputValidationError extends Data.TaggedError(
	"PrismaInputValidationError",
)<{
	cause: PrismaNamespace.PrismaClientKnownRequestError;
	operation: string;
	model: string;
}> {}

export type PrismaCreateError =
	| PrismaValueTooLongError
	| PrismaUniqueConstraintError
	| PrismaForeignKeyConstraintError
	| PrismaDbConstraintError
	| PrismaInputValidationError
	| PrismaMissingRequiredValueError
	| PrismaRelatedRecordNotFoundError
	| PrismaValueOutOfRangeError
	| PrismaConnectionError
	| PrismaTransactionConflictError;

export type PrismaUpdateError =
	| PrismaValueTooLongError
	| PrismaUniqueConstraintError
	| PrismaForeignKeyConstraintError
	| PrismaDbConstraintError
	| PrismaInputValidationError
	| PrismaMissingRequiredValueError
	| PrismaRelationViolationError
	| PrismaRelatedRecordNotFoundError
	| PrismaValueOutOfRangeError
	| PrismaConnectionError
	| PrismaRecordNotFoundError
	| PrismaTransactionConflictError;

export type PrismaDeleteError =
	| PrismaForeignKeyConstraintError
	| PrismaRelationViolationError
	| PrismaConnectionError
	| PrismaRecordNotFoundError
	| PrismaTransactionConflictError;

export type PrismaFindOrThrowError =
	| PrismaConnectionError
	| PrismaRecordNotFoundError;

export type PrismaFindError = PrismaConnectionError;

export type PrismaDeleteManyError =
	| PrismaForeignKeyConstraintError
	| PrismaRelationViolationError
	| PrismaConnectionError
	| PrismaTransactionConflictError;

export type PrismaUpdateManyError =
	| PrismaValueTooLongError
	| PrismaUniqueConstraintError
	| PrismaForeignKeyConstraintError
	| PrismaDbConstraintError
	| PrismaInputValidationError
	| PrismaMissingRequiredValueError
	| PrismaValueOutOfRangeError
	| PrismaConnectionError
	| PrismaTransactionConflictError;

export type PrismaError =
	| PrismaValueTooLongError
	| PrismaUniqueConstraintError
	| PrismaForeignKeyConstraintError
	| PrismaDbConstraintError
	| PrismaInputValidationError
	| PrismaMissingRequiredValueError
	| PrismaRelationViolationError
	| PrismaRelatedRecordNotFoundError
	| PrismaValueOutOfRangeError
	| PrismaConnectionError
	| PrismaRecordNotFoundError
	| PrismaTransactionConflictError;

// Generic mapper for raw operations and fallback
const mapError = (
	error: unknown,
	operation: string,
	model: string,
): PrismaError => {
	if (isPrismaClientKnownRequestError(error)) {
		const knownError = error;
		switch (knownError.code) {
			case "P2000":
				return new PrismaValueTooLongError({ cause: error, operation, model });
			case "P2002":
				return new PrismaUniqueConstraintError({
					cause: error,
					operation,
					model,
				});
			case "P2003":
				return new PrismaForeignKeyConstraintError({
					cause: error,
					operation,
					model,
				});
			case "P2004":
				return new PrismaDbConstraintError({ cause: error, operation, model });
			case "P2005":
			case "P2006":
			case "P2019":
				return new PrismaInputValidationError({
					cause: error,
					operation,
					model,
				});
			case "P2011":
			case "P2012":
				return new PrismaMissingRequiredValueError({
					cause: error,
					operation,
					model,
				});
			case "P2014":
				return new PrismaRelationViolationError({
					cause: error,
					operation,
					model,
				});
			case "P2015":
			case "P2018":
				return new PrismaRelatedRecordNotFoundError({
					cause: error,
					operation,
					model,
				});
			case "P2020":
				return new PrismaValueOutOfRangeError({
					cause: error,
					operation,
					model,
				});
			case "P2024":
				return new PrismaConnectionError({ cause: error, operation, model });
			case "P2025":
				return new PrismaRecordNotFoundError({
					cause: error,
					operation,
					model,
				});
			case "P2034":
				return new PrismaTransactionConflictError({
					cause: error,
					operation,
					model,
				});
		}
	}
	// Unknown errors are not handled and will be treated as defects
	throw error;
};

// Specific mappers to narrow error types per operation

// Create, Upsert
const mapCreateError = (
	error: unknown,
	operation: string,
	model: string,
): PrismaCreateError => {
	if (isPrismaClientKnownRequestError(error)) {
		const knownError = error;
		switch (knownError.code) {
			case "P2000":
				return new PrismaValueTooLongError({ cause: error, operation, model });
			case "P2002":
				return new PrismaUniqueConstraintError({
					cause: error,
					operation,
					model,
				});
			case "P2003":
				return new PrismaForeignKeyConstraintError({
					cause: error,
					operation,
					model,
				});
			case "P2004":
				return new PrismaDbConstraintError({ cause: error, operation, model });
			case "P2005":
			case "P2006":
			case "P2019":
				return new PrismaInputValidationError({
					cause: error,
					operation,
					model,
				});
			case "P2011":
			case "P2012":
				return new PrismaMissingRequiredValueError({
					cause: error,
					operation,
					model,
				});
			case "P2015":
			case "P2018":
				return new PrismaRelatedRecordNotFoundError({
					cause: error,
					operation,
					model,
				});
			case "P2020":
				return new PrismaValueOutOfRangeError({
					cause: error,
					operation,
					model,
				});
			case "P2024":
				return new PrismaConnectionError({ cause: error, operation, model });
			case "P2034":
				return new PrismaTransactionConflictError({
					cause: error,
					operation,
					model,
				});
		}
	}
	throw error;
};

// Update
const mapUpdateError = (
	error: unknown,
	operation: string,
	model: string,
): PrismaUpdateError => {
	if (isPrismaClientKnownRequestError(error)) {
		const knownError = error;
		switch (knownError.code) {
			case "P2000":
				return new PrismaValueTooLongError({ cause: error, operation, model });
			case "P2002":
				return new PrismaUniqueConstraintError({
					cause: error,
					operation,
					model,
				});
			case "P2003":
				return new PrismaForeignKeyConstraintError({
					cause: error,
					operation,
					model,
				});
			case "P2004":
				return new PrismaDbConstraintError({ cause: error, operation, model });
			case "P2005":
			case "P2006":
			case "P2019":
				return new PrismaInputValidationError({
					cause: error,
					operation,
					model,
				});
			case "P2011":
			case "P2012":
				return new PrismaMissingRequiredValueError({
					cause: error,
					operation,
					model,
				});
			case "P2014":
				return new PrismaRelationViolationError({
					cause: error,
					operation,
					model,
				});
			case "P2015":
			case "P2018":
				return new PrismaRelatedRecordNotFoundError({
					cause: error,
					operation,
					model,
				});
			case "P2020":
				return new PrismaValueOutOfRangeError({
					cause: error,
					operation,
					model,
				});
			case "P2024":
				return new PrismaConnectionError({ cause: error, operation, model });
			case "P2025":
				return new PrismaRecordNotFoundError({
					cause: error,
					operation,
					model,
				});
			case "P2034":
				return new PrismaTransactionConflictError({
					cause: error,
					operation,
					model,
				});
		}
	}
	throw error;
};

// Delete
const mapDeleteError = (
	error: unknown,
	operation: string,
	model: string,
): PrismaDeleteError => {
	if (isPrismaClientKnownRequestError(error)) {
		const knownError = error;
		switch (knownError.code) {
			case "P2003":
				return new PrismaForeignKeyConstraintError({
					cause: error,
					operation,
					model,
				});
			case "P2014":
				return new PrismaRelationViolationError({
					cause: error,
					operation,
					model,
				});
			case "P2024":
				return new PrismaConnectionError({ cause: error, operation, model });
			case "P2025":
				return new PrismaRecordNotFoundError({
					cause: error,
					operation,
					model,
				});
			case "P2034":
				return new PrismaTransactionConflictError({
					cause: error,
					operation,
					model,
				});
		}
	}
	throw error;
};

// FindOrThrow
const mapFindOrThrowError = (
	error: unknown,
	operation: string,
	model: string,
): PrismaFindOrThrowError => {
	if (isPrismaClientKnownRequestError(error)) {
		const knownError = error;
		switch (knownError.code) {
			case "P2024":
				return new PrismaConnectionError({ cause: error, operation, model });
			case "P2025":
				return new PrismaRecordNotFoundError({
					cause: error,
					operation,
					model,
				});
		}
	}
	throw error;
};

// Find
const mapFindError = (
	error: unknown,
	operation: string,
	model: string,
): PrismaFindError => {
	if (isPrismaClientKnownRequestError(error)) {
		const knownError = error;
		switch (knownError.code) {
			case "P2024":
				return new PrismaConnectionError({ cause: error, operation, model });
		}
	}
	throw error;
};

// DeleteMany
const mapDeleteManyError = (
	error: unknown,
	operation: string,
	model: string,
): PrismaDeleteManyError => {
	if (isPrismaClientKnownRequestError(error)) {
		const knownError = error;
		switch (knownError.code) {
			case "P2003":
				return new PrismaForeignKeyConstraintError({
					cause: error,
					operation,
					model,
				});
			case "P2014":
				return new PrismaRelationViolationError({
					cause: error,
					operation,
					model,
				});
			case "P2024":
				return new PrismaConnectionError({ cause: error, operation, model });
			case "P2034":
				return new PrismaTransactionConflictError({
					cause: error,
					operation,
					model,
				});
		}
	}
	throw error;
};

// UpdateMany
const mapUpdateManyError = (
	error: unknown,
	operation: string,
	model: string,
): PrismaUpdateManyError => {
	if (isPrismaClientKnownRequestError(error)) {
		const knownError = error;
		switch (knownError.code) {
			case "P2000":
				return new PrismaValueTooLongError({ cause: error, operation, model });
			case "P2002":
				return new PrismaUniqueConstraintError({
					cause: error,
					operation,
					model,
				});
			case "P2003":
				return new PrismaForeignKeyConstraintError({
					cause: error,
					operation,
					model,
				});
			case "P2004":
				return new PrismaDbConstraintError({ cause: error, operation, model });
			case "P2005":
			case "P2006":
			case "P2019":
				return new PrismaInputValidationError({
					cause: error,
					operation,
					model,
				});
			case "P2011":
			case "P2012":
				return new PrismaMissingRequiredValueError({
					cause: error,
					operation,
					model,
				});
			case "P2020":
				return new PrismaValueOutOfRangeError({
					cause: error,
					operation,
					model,
				});
			case "P2024":
				return new PrismaConnectionError({ cause: error, operation, model });
			case "P2034":
				return new PrismaTransactionConflictError({
					cause: error,
					operation,
					model,
				});
		}
	}
	throw error;
};

/** Pin parameter controlling replica routing for a sub-service. */
type PrismaRoutingPin = "primary" | "replica" | undefined;

/** Pick a random replica from the provided array. Caller ensures non-empty. */
const pickReplica = (
	replicas: ReadonlyArray<BasePrismaClient>,
): BasePrismaClient =>
	replicas[Math.floor(Math.random() * replicas.length)] as BasePrismaClient;

/**
 * Resolve the client to use for a READ operation.
 *
 * Priority:
 *   1. Transaction client (if inside `$transaction`)
 *   2. Primary client (if pin === "primary")
 *   3. Random replica (if replicas are configured and non-empty)
 *   4. Primary client (fallback)
 */
const readClient = (client: BasePrismaClient, pin: PrismaRoutingPin) =>
	Effect.gen(function* () {
		const tx = yield* Effect.serviceOption(PrismaTransactionClientService);
		if (Option.isSome(tx)) return tx.value as BasePrismaClient;
		if (pin === "primary") return client;
		const replicas = yield* Effect.serviceOption(PrismaReplicas);
		if (Option.isNone(replicas) || replicas.value.length === 0) return client;
		return pickReplica(replicas.value);
	});

/**
 * Resolve the client to use for a WRITE or raw SQL operation.
 *
 * Priority:
 *   1. Transaction client (if inside `$transaction`)
 *   2. Random replica (only if pin === "replica" AND replicas are configured)
 *   3. Primary client (default - writes/raw always go here unless explicitly pinned to replica)
 */
const writeClient = (client: BasePrismaClient, pin: PrismaRoutingPin) =>
	Effect.gen(function* () {
		const tx = yield* Effect.serviceOption(PrismaTransactionClientService);
		if (Option.isSome(tx)) return tx.value as BasePrismaClient;
		if (pin === "replica") {
			const replicas = yield* Effect.serviceOption(PrismaReplicas);
			if (Option.isSome(replicas) && replicas.value.length > 0) {
				return pickReplica(replicas.value);
			}
		}
		return client;
	});

/**
 * Like Effect.acquireUseRelease, but allows the release function to fail.
 * Release errors are surfaced in the error channel instead of becoming defects.
 *
 * Key properties:
 * - The release function is always called, even if use fails
 * - The release function is uninterruptible to ensure cleanup completes
 * - Release errors are surfaced in the error channel, not as defects
 */
const acquireUseReleaseWithErrors = <A, E, R, A2, E2, R2, X, E3, R3>(
	acquire: EffectType<A, E, R>,
	use: (a: A) => EffectType<A2, E2, R2>,
	release: (a: A, exit: Exit.Exit<A2, E2>) => EffectType<X, E3, R3>,
): EffectType<A2, E | E2 | E3, R | R2 | R3> =>
	Effect.uninterruptibleMask((restore) =>
		Effect.flatMap(acquire, (a) =>
			Effect.flatMap(Effect.exit(restore(use(a))), (exit) =>
				Effect.flatMap(
					// Make release uninterruptible to ensure cleanup always completes
					Effect.exit(Effect.uninterruptible(release(a, exit))),
					(releaseExit) => {
						if (Exit.isFailure(releaseExit)) {
							// Release failed - surface the release error
							return releaseExit as any;
						}
						// Release succeeded - return the original use result
						return exit as any;
					},
				),
			),
		),
	);

/**
 * Interface defining all Prisma operations with explicit types.
 * This provides a type contract that the service implementation must satisfy.
 */
export interface IPrismaService {
	client: BasePrismaClient;
	// Read-replica routing escape hatches
	/**
	 * Return a sub-service where all operations (including reads) are pinned to the primary client.
	 * Useful when you need strong consistency for a specific read (e.g. read-your-own-writes).
	 */
	$primary: () => IPrismaService;
	/**
	 * Return a sub-service where all operations (including writes and raw queries) are pinned to a random replica.
	 * If no replicas are configured, falls back to the primary client.
	 */
	$replica: () => IPrismaService;
	// Transaction operations
	$transaction: <R, E, A>(
		effect: EffectType<A, E, R>,
	) => EffectType<
		A,
		E | PrismaError,
		Exclude<R, PrismaTransactionClientService>
	>;

	$transactionWith: <R, E, A>(
		effect: EffectType<A, E, R>,
		options: TransactionOptions,
	) => EffectType<A, E | PrismaError, R>;

	$isolatedTransaction: <R, E, A>(
		effect: EffectType<A, E, R>,
	) => EffectType<A, E | PrismaError, R>;

	$isolatedTransactionWith: <R, E, A>(
		effect: EffectType<A, E, R>,
		options: TransactionOptions,
	) => EffectType<A, E | PrismaError, R>;

	// Raw SQL operations
	$executeRaw: (
		args: PrismaNamespace.Sql | [PrismaNamespace.Sql, ...any[]],
	) => EffectType<number, PrismaError>;

	$executeRawUnsafe: (
		query: string,
		...values: any[]
	) => EffectType<number, PrismaError>;

	$queryRaw: <T = unknown>(
		args: PrismaNamespace.Sql | [PrismaNamespace.Sql, ...any[]],
	) => EffectType<T, PrismaError>;

	$queryRawUnsafe: <T = unknown>(
		query: string,
		...values: any[]
	) => EffectType<T, PrismaError>;

	// Model operations
	user: {
		findUnique: <A extends UserFindUniqueArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "findUnique"> | null,
			PrismaFindError
		>;

		findUniqueOrThrow: <A extends UserFindUniqueOrThrowArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "findUniqueOrThrow">,
			PrismaFindOrThrowError
		>;

		findFirst: <A extends UserFindFirstArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "findFirst"> | null,
			PrismaFindError
		>;

		findFirstOrThrow: <A extends UserFindFirstOrThrowArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "findFirstOrThrow">,
			PrismaFindOrThrowError
		>;

		findMany: <A extends UserFindManyArgs>(
			args?: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "findMany">,
			PrismaFindError
		>;

		create: <A extends UserCreateArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "create">,
			PrismaCreateError
		>;

		createMany: <A extends UserCreateManyArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "createMany">,
			PrismaCreateError
		>;

		createManyAndReturn: <A extends UserCreateManyAndReturnArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<
				BasePrismaClient["user"],
				A,
				"createManyAndReturn"
			>,
			PrismaCreateError
		>;

		delete: <A extends UserDeleteArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "delete">,
			PrismaDeleteError
		>;

		update: <A extends UserUpdateArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "update">,
			PrismaUpdateError
		>;

		deleteMany: <A extends UserDeleteManyArgs>(
			args?: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "deleteMany">,
			PrismaDeleteError
		>;

		updateMany: <A extends UserUpdateManyArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "updateMany">,
			PrismaUpdateError
		>;

		updateManyAndReturn: <A extends UserUpdateManyAndReturnArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<
				BasePrismaClient["user"],
				A,
				"updateManyAndReturn"
			>,
			PrismaUpdateError
		>;

		upsert: <A extends UserUpsertArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "upsert">,
			PrismaUpdateError
		>;

		count: <A extends UserCountArgs>(
			args?: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "count">,
			PrismaFindError
		>;

		aggregate: <A extends UserAggregateArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "aggregate">,
			PrismaFindError
		>;

		groupBy: <A extends UserGroupByArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["user"], A, "groupBy">,
			PrismaFindError
		>;
	};

	post: {
		findUnique: <A extends PostFindUniqueArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "findUnique"> | null,
			PrismaFindError
		>;

		findUniqueOrThrow: <A extends PostFindUniqueOrThrowArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "findUniqueOrThrow">,
			PrismaFindOrThrowError
		>;

		findFirst: <A extends PostFindFirstArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "findFirst"> | null,
			PrismaFindError
		>;

		findFirstOrThrow: <A extends PostFindFirstOrThrowArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "findFirstOrThrow">,
			PrismaFindOrThrowError
		>;

		findMany: <A extends PostFindManyArgs>(
			args?: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "findMany">,
			PrismaFindError
		>;

		create: <A extends PostCreateArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "create">,
			PrismaCreateError
		>;

		createMany: <A extends PostCreateManyArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "createMany">,
			PrismaCreateError
		>;

		createManyAndReturn: <A extends PostCreateManyAndReturnArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<
				BasePrismaClient["post"],
				A,
				"createManyAndReturn"
			>,
			PrismaCreateError
		>;

		delete: <A extends PostDeleteArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "delete">,
			PrismaDeleteError
		>;

		update: <A extends PostUpdateArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "update">,
			PrismaUpdateError
		>;

		deleteMany: <A extends PostDeleteManyArgs>(
			args?: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "deleteMany">,
			PrismaDeleteError
		>;

		updateMany: <A extends PostUpdateManyArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "updateMany">,
			PrismaUpdateError
		>;

		updateManyAndReturn: <A extends PostUpdateManyAndReturnArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<
				BasePrismaClient["post"],
				A,
				"updateManyAndReturn"
			>,
			PrismaUpdateError
		>;

		upsert: <A extends PostUpsertArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "upsert">,
			PrismaUpdateError
		>;

		count: <A extends PostCountArgs>(
			args?: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "count">,
			PrismaFindError
		>;

		aggregate: <A extends PostAggregateArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "aggregate">,
			PrismaFindError
		>;

		groupBy: <A extends PostGroupByArgs>(
			args: A,
		) => EffectType<
			PrismaNamespace.Result<BasePrismaClient["post"], A, "groupBy">,
			PrismaFindError
		>;
	};
}

/**
 * Internal helper to begin a callback-free interactive transaction.
 * Returns a transaction client with $commit and $rollback methods.
 * This allows transactions to run in the same fiber as the parent effect.
 */
const $begin = (
	client: BasePrismaClient,
	options?: {
		maxWait?: number;
		timeout?: number;
		isolationLevel?: PrismaNamespace.TransactionIsolationLevel;
	},
): EffectType<FlatTransactionClient, PrismaError> =>
	Effect.callback<FlatTransactionClient, PrismaError>((resume) => {
		let setTxClient: (txClient: PrismaNamespace.TransactionClient) => void;
		let commit: () => void;
		let rollback: () => void;

		// Promise that resolves when we get the transaction client
		const txClientPromise = new Promise<PrismaNamespace.TransactionClient>(
			(res) => {
				setTxClient = res;
			},
		);

		// Promise that controls when the transaction commits/rolls back
		const txPromise = new Promise<void>((_res, _rej) => {
			commit = () => _res(undefined);
			rollback = () => _rej(ROLLBACK);
		});

		// Start the transaction - Prisma will wait on txPromise before committing
		const tx = client
			.$transaction((txClient) => {
				setTxClient(txClient);
				return txPromise;
			}, options)
			.catch((e) => {
				// Swallow intentional rollbacks, rethrow actual errors
				if (e === ROLLBACK) return;
				throw e;
			});

		// Once we have the transaction client, wrap it with commit/rollback methods
		txClientPromise
			.then((innerTx) => {
				const proxy = new Proxy(innerTx, {
					get(target, prop) {
						if (prop === "$commit")
							return () => {
								commit();
								return tx;
							};
						if (prop === "$rollback")
							return () => {
								rollback();
								return tx;
							};
						return target[prop as keyof typeof target];
					},
				}) as FlatTransactionClient;
				resume(Effect.succeed(proxy));
			})
			.catch((error) => {
				resume(Effect.fail(mapError(error, "$transaction", "Prisma")));
			});
	});

/**
 * The main Prisma service with all database operations.
 * Provides type-safe, effectful access to your Prisma models.
 *
 * @example
 * const program = Effect.gen(function* () {
 *   const prisma = yield* Prisma
 *   const user = yield* prisma.user.create({ data: { name: "Alice" } })
 *   return user
 * })
 *
 * // Run with layer
 * Effect.runPromise(program.pipe(Effect.provide(Prisma.layer({ datasourceUrl: "..." }))))
 */
const makePrismaService = Effect.gen(function* () {
	const client = yield* PrismaClient;

	let primaryService!: IPrismaService;
	let replicaService!: IPrismaService;

	const buildService = (pin: PrismaRoutingPin): IPrismaService => ({
		client,
		$primary: () => primaryService,
		$replica: () => replicaService,
		/**
		 * Execute an effect within a database transaction.
		 * All operations within the effect will be atomic - they either all succeed or all fail.
		 *
		 * This implementation uses a callback-free transaction pattern that keeps the effect
		 * running in the same fiber as the parent, preserving Ref, FiberRef, and Context access.
		 *
		 * Uses default transaction options from PrismaClient constructor.
		 * For custom options, use `$transactionWith`.
		 *
		 * @example
		 * const result = yield* prisma.$transaction(
		 *   Effect.gen(function* () {
		 *     const user = yield* prisma.user.create({ data: { name: "Alice" } })
		 *     yield* prisma.post.create({ data: { title: "Hello", authorId: user.id } })
		 *     return user
		 *   })
		 * )
		 */
		$transaction: Effect.fnUntraced(function* (effect) {
			const currentTx = yield* Effect.serviceOption(
				PrismaTransactionClientService,
			);

			// If already in a transaction, just run the effect
			if (Option.isSome(currentTx)) {
				return yield* effect as EffectType<
					EffectSuccess<typeof effect>,
					EffectError<typeof effect>,
					Exclude<EffectServices<typeof effect>, PrismaTransactionClientService>
				>;
			}

			// Otherwise, start a new transaction
			return yield* acquireUseReleaseWithErrors(
				// Acquire: begin a new transaction with default options
				$begin(client),

				// Use: run the effect with the transaction client injected
				(txClient) =>
					effect.pipe(
						Effect.provideService(PrismaTransactionClientService, txClient),
					),

				// Release: commit on success, rollback on failure/interruption
				(txClient, exit) =>
					Exit.isSuccess(exit)
						? Effect.tryPromise({
								try: () => txClient.$commit(),
								catch: (error) => mapError(error, "$commit", "Prisma"),
							}).pipe(Effect.withSpan("txClient.$rollback"))
						: Effect.tryPromise({
								try: () => txClient.$rollback(),
								catch: (error) => mapError(error, "$rollback", "Prisma"),
							}).pipe(Effect.withSpan("txClient.$rollback")),
			);
		}),

		/**
		 * Execute an effect within a database transaction with custom options.
		 * All operations within the effect will be atomic - they either all succeed or all fail.
		 *
		 * This implementation uses a callback-free transaction pattern that keeps the effect
		 * running in the same fiber as the parent, preserving Ref, FiberRef, and Context access.
		 *
		 * Options passed here override any defaults set in PrismaClient constructor.
		 *
		 * @example
		 * // Override default isolation level for this transaction
		 * const result = yield* prisma.$transactionWith(
		 *   Effect.gen(function* () {
		 *     const user = yield* prisma.user.create({ data: { name: "Alice" } })
		 *     yield* prisma.post.create({ data: { title: "Hello", authorId: user.id } })
		 *     return user
		 *   }),
		 *   { isolationLevel: "ReadCommitted", timeout: 10000 }
		 * )
		 */
		$transactionWith: Effect.fnUntraced(function* (effect, options) {
			const currentTx = yield* Effect.serviceOption(
				PrismaTransactionClientService,
			);

			// If already in a transaction, just run the effect
			if (Option.isSome(currentTx)) {
				return yield* effect;
			}

			// Otherwise, start a new transaction
			return yield* acquireUseReleaseWithErrors(
				// Acquire: begin a new transaction
				// Prisma merges per-call options with constructor defaults internally
				$begin(client, options),

				// Use: run the effect with the transaction client injected
				(txClient) =>
					effect.pipe(
						Effect.provideService(PrismaTransactionClientService, txClient),
					),

				// Release: commit on success, rollback on failure/interruption
				(txClient, exit) =>
					Exit.isSuccess(exit)
						? Effect.tryPromise({
								try: () => txClient.$commit(),
								catch: (error) => mapError(error, "$commit", "Prisma"),
							}).pipe(Effect.withSpan("txClient.$rollback"))
						: Effect.tryPromise({
								try: () => txClient.$rollback(),
								catch: (error) => mapError(error, "$rollback", "Prisma"),
							}).pipe(Effect.withSpan("txClient.$rollback")),
			);
		}),

		/**
		 * Execute an effect in a NEW transaction, even if already inside a transaction.
		 * Unlike `$transaction`, this always creates a fresh, independent transaction.
		 *
		 * Use this for operations that should NOT be rolled back with the parent:
		 * - Audit logging that must persist even if main operation fails
		 * - Saga pattern where each step has independent commit/rollback
		 * - Background job queuing that should commit immediately
		 *
		 * ⚠️ WARNING: The isolated transaction can commit while the parent rolls back,
		 * or vice versa. Use carefully to avoid data inconsistencies.
		 *
		 * Uses default transaction options from PrismaClient constructor.
		 * For custom options, use `$isolatedTransactionWith`.
		 *
		 * @example
		 * yield* prisma.$transaction(
		 *   Effect.gen(function* () {
		 *     // This audit log commits independently - survives parent rollback
		 *     yield* prisma.$isolatedTransaction(
		 *       prisma.auditLog.create({ data: { action: "attempt", userId } })
		 *     )
		 *     // Main operation - if this fails, audit log is still committed
		 *     yield* prisma.user.delete({ where: { id: userId } })
		 *   })
		 * )
		 */
		$isolatedTransaction: Effect.fnUntraced(function* (effect) {
			// Always create a fresh transaction
			return yield* acquireUseReleaseWithErrors(
				$begin(client),
				(txClient) =>
					effect.pipe(
						Effect.provideService(PrismaTransactionClientService, txClient),
					),
				(txClient, exit) =>
					Exit.isSuccess(exit)
						? Effect.tryPromise({
								try: () => txClient.$commit(),
								catch: (error) => mapError(error, "$commit", "Prisma"),
							}).pipe(Effect.withSpan("txClient.$rollback"))
						: Effect.tryPromise({
								try: () => txClient.$rollback(),
								catch: (error) => mapError(error, "$rollback", "Prisma"),
							}).pipe(Effect.withSpan("txClient.$rollback")),
			);
		}),

		/**
		 * Execute an effect in a NEW transaction with custom options, even if already inside a transaction.
		 * Unlike `$transaction`, this always creates a fresh, independent transaction.
		 *
		 * Use this for operations that should NOT be rolled back with the parent:
		 * - Audit logging that must persist even if main operation fails
		 * - Saga pattern where each step has independent commit/rollback
		 * - Background job queuing that should commit immediately
		 *
		 * ⚠️ WARNING: The isolated transaction can commit while the parent rolls back,
		 * or vice versa. Use carefully to avoid data inconsistencies.
		 *
		 * Options passed here override any defaults set in PrismaClient constructor.
		 *
		 * @example
		 * yield* prisma.$transaction(
		 *   Effect.gen(function* () {
		 *     // This audit log commits independently with custom isolation level
		 *     yield* prisma.$isolatedTransactionWith(
		 *       prisma.auditLog.create({ data: { action: "attempt", userId } }),
		 *       { isolationLevel: "Serializable" }
		 *     )
		 *     // Main operation - if this fails, audit log is still committed
		 *     yield* prisma.user.delete({ where: { id: userId } })
		 *   })
		 * )
		 */
		$isolatedTransactionWith: Effect.fnUntraced(function* (effect, options) {
			// Always create a fresh transaction
			return yield* acquireUseReleaseWithErrors(
				$begin(client, options),
				(txClient) =>
					effect.pipe(
						Effect.provideService(PrismaTransactionClientService, txClient),
					),
				(txClient, exit) =>
					Exit.isSuccess(exit)
						? Effect.tryPromise({
								try: () => txClient.$commit(),
								catch: (error) => mapError(error, "$commit", "Prisma"),
							}).pipe(Effect.withSpan("txClient.$rollback"))
						: Effect.tryPromise({
								try: () => txClient.$rollback(),
								catch: (error) => mapError(error, "$rollback", "Prisma"),
							}).pipe(Effect.withSpan("txClient.$rollback")),
			);
		}),

		$executeRaw: Effect.fnUntraced(function* (args) {
			const actualClient = yield* writeClient(client, pin);
			return yield* Effect.tryPromise<any, PrismaError>({
				try: () =>
					(Array.isArray(args)
						? actualClient.$executeRaw(args[0], ...args.slice(1))
						: actualClient.$executeRaw(args)) as any,
				catch: (error) => mapError(error, "$executeRaw", "Prisma"),
			});
		}),

		$executeRawUnsafe: Effect.fnUntraced(function* (query, ...values) {
			const actualClient = yield* writeClient(client, pin);
			return yield* Effect.tryPromise<any, PrismaError>({
				try: () => actualClient.$executeRawUnsafe(query, ...values) as any,
				catch: (error) => mapError(error, "$executeRawUnsafe", "Prisma"),
			});
		}),

		$queryRaw: Effect.fnUntraced(function* (args) {
			const actualClient = yield* writeClient(client, pin);
			return yield* Effect.tryPromise<any, PrismaError>({
				try: () =>
					(Array.isArray(args)
						? actualClient.$queryRaw(args[0], ...args.slice(1))
						: actualClient.$queryRaw(args)) as any,
				catch: (error) => mapError(error, "$queryRaw", "Prisma"),
			});
		}),

		$queryRawUnsafe: Effect.fnUntraced(function* (query, ...values) {
			const actualClient = yield* writeClient(client, pin);
			return yield* Effect.tryPromise<any, PrismaError>({
				try: () => actualClient.$queryRawUnsafe(query, ...values) as any,
				catch: (error) => mapError(error, "$queryRawUnsafe", "Prisma"),
			});
		}),

		user: {
			findUnique: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindError>({
					try: () => actualClient.user.findUnique(args as any) as any,
					catch: (error) => mapFindError(error, "findUnique", "User"),
				});
			}),

			findUniqueOrThrow: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindOrThrowError>({
					try: () => actualClient.user.findUniqueOrThrow(args as any) as any,
					catch: (error) =>
						mapFindOrThrowError(error, "findUniqueOrThrow", "User"),
				});
			}),

			findFirst: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindError>({
					try: () => actualClient.user.findFirst(args as any) as any,
					catch: (error) => mapFindError(error, "findFirst", "User"),
				});
			}),

			findFirstOrThrow: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindOrThrowError>({
					try: () => actualClient.user.findFirstOrThrow(args as any) as any,
					catch: (error) =>
						mapFindOrThrowError(error, "findFirstOrThrow", "User"),
				});
			}),

			findMany: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindError>({
					try: () => actualClient.user.findMany(args as any) as any,
					catch: (error) => mapFindError(error, "findMany", "User"),
				});
			}),

			create: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaCreateError>({
					try: () => actualClient.user.create(args as any) as any,
					catch: (error) => mapCreateError(error, "create", "User"),
				});
			}),

			createMany: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaCreateError>({
					try: () => actualClient.user.createMany(args as any),
					catch: (error) => mapCreateError(error, "createMany", "User"),
				});
			}),

			createManyAndReturn: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaCreateError>({
					try: () => actualClient.user.createManyAndReturn(args as any) as any,
					catch: (error) =>
						mapCreateError(error, "createManyAndReturn", "User"),
				});
			}),

			delete: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaDeleteError>({
					try: () => actualClient.user.delete(args as any) as any,
					catch: (error) => mapDeleteError(error, "delete", "User"),
				});
			}),

			update: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaUpdateError>({
					try: () => actualClient.user.update(args as any) as any,
					catch: (error) => mapUpdateError(error, "update", "User"),
				});
			}),

			deleteMany: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaDeleteManyError>({
					try: () => actualClient.user.deleteMany(args as any),
					catch: (error) => mapDeleteManyError(error, "deleteMany", "User"),
				});
			}),

			updateMany: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaUpdateManyError>({
					try: () => actualClient.user.updateMany(args as any),
					catch: (error) => mapUpdateManyError(error, "updateMany", "User"),
				});
			}),

			updateManyAndReturn: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaUpdateManyError>({
					try: () => actualClient.user.updateManyAndReturn(args as any) as any,
					catch: (error) =>
						mapUpdateManyError(error, "updateManyAndReturn", "User"),
				});
			}),

			upsert: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaCreateError>({
					try: () => actualClient.user.upsert(args as any) as any,
					catch: (error) => mapCreateError(error, "upsert", "User"),
				});
			}),

			// Aggregation operations
			count: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindError>({
					try: () => actualClient.user.count(args as any) as any,
					catch: (error) => mapFindError(error, "count", "User"),
				});
			}),

			aggregate: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindError>({
					try: () => actualClient.user.aggregate(args as any) as any,
					catch: (error) => mapFindError(error, "aggregate", "User"),
				});
			}),

			groupBy: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindError>({
					try: () => actualClient.user.groupBy(args as any) as any,
					catch: (error) => mapFindError(error, "groupBy", "User"),
				});
			}),
		},

		post: {
			findUnique: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindError>({
					try: () => actualClient.post.findUnique(args as any) as any,
					catch: (error) => mapFindError(error, "findUnique", "Post"),
				});
			}),

			findUniqueOrThrow: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindOrThrowError>({
					try: () => actualClient.post.findUniqueOrThrow(args as any) as any,
					catch: (error) =>
						mapFindOrThrowError(error, "findUniqueOrThrow", "Post"),
				});
			}),

			findFirst: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindError>({
					try: () => actualClient.post.findFirst(args as any) as any,
					catch: (error) => mapFindError(error, "findFirst", "Post"),
				});
			}),

			findFirstOrThrow: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindOrThrowError>({
					try: () => actualClient.post.findFirstOrThrow(args as any) as any,
					catch: (error) =>
						mapFindOrThrowError(error, "findFirstOrThrow", "Post"),
				});
			}),

			findMany: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindError>({
					try: () => actualClient.post.findMany(args as any) as any,
					catch: (error) => mapFindError(error, "findMany", "Post"),
				});
			}),

			create: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaCreateError>({
					try: () => actualClient.post.create(args as any) as any,
					catch: (error) => mapCreateError(error, "create", "Post"),
				});
			}),

			createMany: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaCreateError>({
					try: () => actualClient.post.createMany(args as any),
					catch: (error) => mapCreateError(error, "createMany", "Post"),
				});
			}),

			createManyAndReturn: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaCreateError>({
					try: () => actualClient.post.createManyAndReturn(args as any) as any,
					catch: (error) =>
						mapCreateError(error, "createManyAndReturn", "Post"),
				});
			}),

			delete: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaDeleteError>({
					try: () => actualClient.post.delete(args as any) as any,
					catch: (error) => mapDeleteError(error, "delete", "Post"),
				});
			}),

			update: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaUpdateError>({
					try: () => actualClient.post.update(args as any) as any,
					catch: (error) => mapUpdateError(error, "update", "Post"),
				});
			}),

			deleteMany: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaDeleteManyError>({
					try: () => actualClient.post.deleteMany(args as any),
					catch: (error) => mapDeleteManyError(error, "deleteMany", "Post"),
				});
			}),

			updateMany: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaUpdateManyError>({
					try: () => actualClient.post.updateMany(args as any),
					catch: (error) => mapUpdateManyError(error, "updateMany", "Post"),
				});
			}),

			updateManyAndReturn: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaUpdateManyError>({
					try: () => actualClient.post.updateManyAndReturn(args as any) as any,
					catch: (error) =>
						mapUpdateManyError(error, "updateManyAndReturn", "Post"),
				});
			}),

			upsert: Effect.fnUntraced(function* (args) {
				const actualClient = yield* writeClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaCreateError>({
					try: () => actualClient.post.upsert(args as any) as any,
					catch: (error) => mapCreateError(error, "upsert", "Post"),
				});
			}),

			// Aggregation operations
			count: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindError>({
					try: () => actualClient.post.count(args as any) as any,
					catch: (error) => mapFindError(error, "count", "Post"),
				});
			}),

			aggregate: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindError>({
					try: () => actualClient.post.aggregate(args as any) as any,
					catch: (error) => mapFindError(error, "aggregate", "Post"),
				});
			}),

			groupBy: Effect.fnUntraced(function* (args) {
				const actualClient = yield* readClient(client, pin);
				return yield* Effect.tryPromise<any, PrismaFindError>({
					try: () => actualClient.post.groupBy(args as any) as any,
					catch: (error) => mapFindError(error, "groupBy", "Post"),
				});
			}),
		},
	});

	primaryService = buildService("primary");
	replicaService = buildService("replica");
	return buildService(undefined);
});

export class Prisma extends Context.Service<Prisma, IPrismaService>()(
	"Prisma",
) {
	/**
	 * Effect that constructs the Prisma service.
	 * Used internally by layer constructors.
	 */
	static make: EffectType<IPrismaService, never, PrismaClient> =
		makePrismaService;
	static Default: Layer.Layer<Prisma, never, PrismaClient> = Layer.effect(
		Prisma,
		this.make,
	);

	/**
	 * Create a complete Prisma layer with the given PrismaClient options.
	 * This is the recommended way to create a Prisma layer - it bundles both
	 * PrismaClient and Prisma service together.
	 *
	 * Pass options directly - the signature matches PrismaClient's constructor.
	 * Prisma 7: requires either `adapter` or `accelerateUrl`
	 *
	 * @example
	 * // Prisma 7 with adapter
	 * const MainLayer = Prisma.layer({ adapter: myAdapter })
	 *
	 * // With transaction options
	 * const MainLayer = Prisma.layer({
	 *   adapter: myAdapter,
	 *   transactionOptions: { isolationLevel: "Serializable" }
	 * })
	 *
	 * // Use it
	 * Effect.runPromise(program.pipe(Effect.provide(MainLayer)))
	 */
	static layer: (
		...args: ConstructorParameters<typeof BasePrismaClient>
	) => Layer.Layer<Prisma | PrismaClient, never, never> = (...args) =>
		this.Default.pipe(Layer.provideMerge(PrismaClient.layer(...args)));

	/**
	 * Create a complete Prisma layer where PrismaClient options are computed via an Effect.
	 * This is useful when you need to fetch configuration or create adapters using Effect.
	 *
	 * @example
	 * // Get config from a service
	 * const MainLayer = Prisma.layerEffect(
	 *   Effect.gen(function* () {
	 *     const config = yield* ConfigService
	 *     return { adapter: createAdapter(config.databaseUrl) }
	 *   })
	 * )
	 *
	 * // With transaction options
	 * const MainLayer = Prisma.layerEffect(
	 *   Effect.gen(function* () {
	 *     return {
	 *       adapter: myAdapter,
	 *       transactionOptions: { isolationLevel: "Serializable" }
	 *     }
	 *   })
	 * )
	 */
	static layerEffect: <R, E>(
		optionsEffect: EffectType<
			ConstructorParameters<typeof BasePrismaClient>[0],
			E,
			R
		>,
	) => Layer.Layer<Prisma | PrismaClient, E, Exclude<R, Scope.Scope>> = (
		optionsEffect,
	) =>
		this.Default.pipe(
			Layer.provideMerge(PrismaClient.layerEffect(optionsEffect)),
		);
}
